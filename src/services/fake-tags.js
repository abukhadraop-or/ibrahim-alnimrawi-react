const tags = {
  tags: ['reactjs', 'angularjs'],
};
/**
 * Get array of popular tags.
 *
 * @return {Promise<String[]>} Promise fulfilled with tags.
 */
export const getTags = () => Promise.resolve(tags);
